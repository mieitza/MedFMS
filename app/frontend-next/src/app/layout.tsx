import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: {
    default: "MedFMS - Fleet Management System",
    template: "%s | MedFMS",
  },
  description: "Medical Fleet Management System - Manage vehicles, drivers, fuel, and maintenance",
  keywords: ["fleet management", "vehicles", "drivers", "maintenance", "fuel tracking"],
  authors: [{ name: "MedFMS Team" }],
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ro" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
